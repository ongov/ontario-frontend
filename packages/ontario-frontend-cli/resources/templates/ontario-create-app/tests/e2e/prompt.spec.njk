/**
 * E2E test prompts to give user feedback about the command structure and how to run the tets.
 */
let E2E_URL = process.env.E2E_URL;

const reset = '\x1b[0m';
const fgCyan = '\x1b[36m';
const fgYellow = '\x1b[33m';
const fgRed = '\x1b[31m';

// In case the required environment variable does not exist
if (!E2E_URL) {
  console.log(`\n${fgRed}The End-To-End page URL does not exist, or was not provided using the "--url" flag.${reset}`)
};

console.log(
  `\n${fgYellow}Make sure to run the project locally or have a working URL before running these End-To-End tests.${reset}`
);
console.log(`\nThese tests use Google Chrome, Firefox, or Microsoft Edge.`);
console.log(
  `The E2E tests will run on the saved URL in the environment variables: ${fgCyan}E2E_URL${reset}=${fgCyan}(your-saved-URL)${reset}`
);
console.log(
  `You can update the ${fgCyan}E2E_URL${reset} value in your local ${fgCyan}.env${reset} file.`
);
console.log(
  `If the local ${fgCyan}.env${reset} file does note exist in your project's root directory, create the ${fgCyan}.env${reset} file, and add the variable:`
);
console.log(
  `Example: "${fgCyan}E2E_URL${reset}=${fgCyan}http://localhost:8080${reset}"`
);
console.log(
  `Make sure the ${fgCyan}E2E_URL${reset} value is set to a running page link of this application.${reset}`
);
console.log(
  `\nTo run the E2E tests on a target URL instead using the command line, run: ${fgYellow}npm run test:e2e ${fgCyan}--url${reset}=${fgCyan}(your-target-URL)${reset}`
);
console.log(
  `Example: ${fgYellow}npm run test:e2e${reset} ${fgCyan}--url${reset}=${fgCyan}https://www.ontario.ca/international-trade-events-calendar${reset}\n`
);
