{
  "name": "{{ projectName }}",
  "version": "0.0.1",
  "description": "{{ projectDescription }}",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf dist && eleventy",
    "debug": "rm -rf dist && DEBUG=* eleventy",
    "serve": "eleventy --serve"{% if esLint %},
    "lint": "./node_modules/.bin/eslint '**/*.js'"{% endif %}{% if prettier %},
    "format": "npx prettier --write ."{% endif %}
  },
  "author": "TODO changeme",
  "license": "Apache 2.0",
  "devDependencies": {
      "@11ty/eleventy": "^1.0.1"{% if esLint %},
      "eslint": "latest",
      "eslint-config-ontario-frontend": "latest"{% endif %}{% if prettier %},
      "prettier": "latest",
      "prettier-config-ontario-frontend": "latest"{% endif %}
  }
}